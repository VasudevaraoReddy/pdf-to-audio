# Copy to .env.production and set your deployed backend URL (from Render).
# Vite bakes this into the build, so the hosted frontend calls the right API.
VITE_API_BASE=https://pdf-to-audio-api.onrender.com
